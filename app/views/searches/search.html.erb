<div class ="container">
  <!--books一覧 -->
  　<% if @range == '2' %>
    <h2>Books search for "<%= @word %>" </h2>
    <%= render 'books/table', books: @book %>
    <% else %>
    <h2>Users search for " <%= @word %> " </h2>
    <%= render 'users/table', user: @user %>
    <% end %>